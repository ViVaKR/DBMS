--* FOR LOOP ( => Instead `WHILE LOOP`)

/* 
    DECLARE @cnt INT = 0
    DECLARE @total INT = 10
    WHILE @cnt < @total
    BEGIN
        { ...statements... }
        SET @cnt = @cnt + 1
    END

 */
